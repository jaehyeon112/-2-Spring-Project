package com.bongsamaru.common.Web;



import org.mybatis.logging.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;

import com.bongsamaru.common.VO.SearchCriteriaVO;
import com.bongsamaru.facility.Service.FacilityService;


import ch.qos.logback.classic.Logger;

@Controller
public class CateriaController {
	/*
	 * @Inject FacilityService facilityService;
	 * 
	 * private static final Logger logger =
	 * LoggerFactory.getLogger(CateriaController.class);
	 * 
	 * @GetMapping() public void SerchList(SearchCriteriaVO cri, Model model) {
	 * logger.info("listGet is callied.....");
	 * logger.info("SearchCriteriaVO: "+cri.toString());
	 * 
	 * model.addAllAttribute("list", facilityService.getFacilityList(cri)); return }
	 */
}
